plc:
  id: 1
  enable: yes
  rateMilliseconds: 1
  file: ${PLC_PATH}test.plc
  macros: TYPE=1,LIMIT=10000
  code:
  - "if(static.counter_app>${LIMIT}){println('PLC:    Appended');static.counter_app:=0;};" # calculate set pos for physical axis
  - static.counter_app:=static.counter_app+1;
